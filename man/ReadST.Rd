% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ReadST.R
\name{ReadST}
\alias{ReadST}
\title{Read and Create Seurat Object from Visium Spatial Transcriptomics Data}
\usage{
ReadST(
  data.dir,
  image.type = c("lowres", "hires"),
  filename = "filtered_feature_bc_matrix.h5",
  assay = "Spatial",
  slice = "slice1",
  bin.size = NULL,
  filter.matrix = TRUE,
  to.upper = FALSE,
  image = NULL,
  ...
)
}
\arguments{
\item{data.dir}{A string specifying the path to the Visium data directory. Can also point to binned output folder.}

\item{image.type}{Image resolution to load. Choose from \code{"lowres"} or \code{"hires"}. Default is \code{"lowres"}.}

\item{filename}{Name of the HDF5 count matrix file. Default is \code{"filtered_feature_bc_matrix.h5"}.}

\item{assay}{Name of the assay to use in the Seurat object. Default is \code{"Spatial"}.}

\item{slice}{Name of the tissue slice. Used as the image key in the Seurat object. Default is \code{"slice1"}.}

\item{bin.size}{Optional. If given (e.g., \code{c(16, 8)}), will read from binned_outputs folder using specified bin size.}

\item{filter.matrix}{Logical. Whether to filter the matrix to spots within tissue. Passed to \code{Read10X_Image()}. Default is \code{TRUE}.}

\item{to.upper}{Logical. Whether to convert gene names to uppercase. Default is \code{FALSE}.}

\item{image}{Optional. A custom image object (e.g., from \code{Read10X_Image}). If provided, it overrides image loading from disk.}

\item{...}{Additional arguments passed to \code{Read10X_h5()}.}
}
\value{
A Seurat object containing the spatial transcriptomics data, including associated image.
}
\description{
This function reads 10X Genomics Visium spatial transcriptomics data,
optionally supports binned data, loads the image, and returns a Seurat object.
}
